name: Pull Request CI

on:
  pull_request:
    branches:
      - main    # Triggers when a pull request targets the `main` branch

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        run: echo "hi from pull request"
  test2:
    runs-on: ubuntu-latest
    steps:
    - run: echo "hi my anme is mohammeed"